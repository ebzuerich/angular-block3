import { InputDateParserDirective } from './input-date-parser.directive';

describe('InputDateParserDirective', () => {
  it('should create an instance', () => {
    const directive = new InputDateParserDirective();
    expect(directive).toBeTruthy();
  });
});
